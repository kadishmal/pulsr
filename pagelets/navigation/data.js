define(function() {
    // list of menus. Each item will be converted to <a> link.
    // It's key will become its href, and value its text.
    return [
        {href: '/docs/app.html', text: 'Docs'}
    ];
});